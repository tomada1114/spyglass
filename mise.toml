# Pinned CLI tool versions — the reproducibility analog of uv's lockfile for dev tools.
# Bump deliberately (see .claude/rules/project.md), never use "latest".
[tools]
xcodegen = "2.45.4"
swiftlint = "0.65.0"
swiftformat = "0.61.1"
xcbeautify = "3.2.1"
just = "1.55.1"
actionlint = "1.7.12"
shellcheck = "0.11.0"
